require 'spec_helper'

describe BitBroker::Broker do
  context "subscriber" do
    it "send data correctly"
  end

  context "publisher" do
    it "receive data correctly"
    it "couldn't receive data with invalid key"
  end
end
